[workspace]
resolver = "3"
members = ["amrust", "amrust_lib"]

# only because that current instant-xml is used like this in 3mf-rs
[patch.crates-io]
instant-xml = { git = "https://github.com/djc/instant-xml.git" }
